{
    "{{ image_id }}": [
        {
            "image_type": "{{ image_type }}",
            "patches": "{{ patch_numbers }}", 
            "nonrolling_patches": "",
            "groups": "{{ osdbagrp_names }}" 
        }
    ]
}
