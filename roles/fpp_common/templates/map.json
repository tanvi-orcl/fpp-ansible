{
    "{{ image_tag }}": [
        {
            "version": "{{ version }}",
            "ru_version": "{{ ru_version }}",
            "aru_id": "226",
            "image_type": "{{ image_type }}",
            "image_name": "{{ image_name }}.zip.gpg",
            "image": "{{ image_name }}.zip.gpg",
            "patches": "{{ patches }}", 
            "lspatches_bugs": "/u01/app/rhp/admin/maps/{{ bug_path }}", 
            "nonrolling_patches": "",
            "groups": "{{ groups }}" 
        }
    ]
}
