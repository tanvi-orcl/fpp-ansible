- name: 'Check FPP Working Copy'
  become: 'true'
  become_user: '{{ user }}'
  become_method: 'sudo'
  shell: |
    /{{ user_mount_point }}/app/rhp/rhphome/bin/rhpctl query workingcopy
  register: check_working_copy_result
- debug:
    msg: "{{ check_working_copy_result }}"