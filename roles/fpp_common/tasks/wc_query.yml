- name: 'Query Working Copy'
  become: 'true'
  become_user: 'oracle'
  become_method: 'sudo'
  shell: |
    {{ rhpctl_path }} query workingcopy -workingcopy {{ wc_name }}
  register: 'query_wc_result'
  ignore_errors: '{{ ignore_errors_flag }}'
- debug:
    msg: '{{ query_wc_result }}'
